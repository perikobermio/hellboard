name: hellboard
description: Moon board of the hell

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_bluetooth_serial: ^0.4.0
  http: ^0.13.5
  select_form_field: ^2.2.0
  firebase_core: ^2.4.1
  firebase_database: ^10.0.9
  path_provider: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/vias.json
    - assets/panel40.json
    - assets/img/panel40.png
    - assets/img/panel40_dev.png
